# Environment variables example for Tier of Legends
# Copy to `.env.local` or `.env` and fill in values

# Required
NEXTAUTH_SECRET=changeme-long-random-string
MONGODB_URI=mongodb+srv://<user>:<password>@<cluster>/<db>?retryWrites=true&w=majority
MONGODB_DB=tier-of-legends

# App base URL (used by NextAuth and metadata). For local dev:
NEXTAUTH_URL=http://localhost:3000

# Feature flags
# Set to 'true' to bypass auth (development only)
DISABLE_AUTH=false

# Testing
# Base URL used by Playwright tests
PLAYWRIGHT_BASE_URL=http://localhost:3000

# Optional / platform-provided
# VERCEL_URL=your-app.vercel.app
# VERCEL_ENV=development
# AUTH_SECRET= # alternative to NEXTAUTH_SECRET if preferred
# NEXT_SERVER_ACTIONS_ENCRYPTION_KEY= # base64url-encoded 32-byte key (optional)
